# Sensor simulator

This tool will generate realistic Krebb One measurement packets for iOS development and integration testing without connected hardware.

It must emit data that conforms to [the shared sensor protocol](../../docs/sensor-protocol.md), including missing readings and varying quality values.
